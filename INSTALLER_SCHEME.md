# 局域网客户端安装包方案

## 目标

把当前 `PyQt5 + MySQL` 桌面程序整理成一个 **Windows 安装包**，实现：

- 目标电脑无需预装 Python
- 可选择安装路径
- 自动创建桌面快捷方式
- 安装后可直接运行
- 首次启动可配置为 **客户端** 或 **主机**
- 局域网内多台电脑访问同一份数据库和共享图片目录

## 推荐打包路线

### 方案：`PyInstaller + Inno Setup`

- **PyInstaller**：把 Python 程序和依赖打成 Windows 可运行目录
- **Inno Setup**：把 PyInstaller 输出目录封装成安装包 `setup.exe`

### 为什么推荐 `onedir`，不推荐 `onefile`

当前项目里有较多 **磁盘上的相对资源路径**，例如：

- `main_ui/assets/icons`
- `Auto_complete Settings`
- 图片共享目录配置

因此更适合用 **`PyInstaller onedir`**，原因：

- 资源文件结构更稳定
- 路径兼容性更好
- 首次部署和排错更容易
- 安装包更新也更直观

`onefile` 也能做，但后续资源定位和调试成本更高，不建议作为第一版安装包。

## 安装包的角色划分

### 1. 统一应用安装包

建议先做 **一个安装包**，安装后通过软件内部配置决定角色：

- **主机**：
  - 安装包只负责安装程序
  - 第一次运行后，在“服务器配置”中点 **主机初始化**
  - 自动创建数据库、表结构、默认模板
  - 配置共享图片目录

- **客户端**：
  - 只安装程序
  - 在“服务器配置”中填写主机 IP、数据库信息、共享图片目录
  - 不做数据库初始化

### 2. 不建议把 MySQL 一起打进安装包

当前更推荐：

- **MySQL 独立部署在主机上**
- 客户端只连接主机数据库
- 图片文件放到局域网共享目录

这样维护成本最低，也更符合现有代码结构。

## 安装包应包含的内容

### 代码与资源

- `enter.py`
- `common/`
- `login_ui/`
- `main_ui/`
- `main_ui/assets/icons/`
- `Auto_complete Settings/`

### 运行时可选资源

- `data/images/`：作为本地开发/兜底目录
- `pfms.sql`：可保留为备份参考，不作为主安装依赖
- 目录样表、示例数据等：按需要决定是否捆绑

## 局域网模式下的运行约定

### 数据库

- 主机电脑提供 MySQL
- 其他客户端连接主机的局域网 IP
- 数据库连接信息通过软件内“服务器配置”保存

### 图片

- 图片必须放在 **共享目录**
- 共享目录可以是：
  - `\\主机IP\cadre_images`
  - 或映射成网络盘
- 软件已支持：
  - 保存共享目录配置
  - 从共享目录读取图片
  - 旧图片路径回退解析
  - 首次初始化时迁移历史图片到共享目录

## 首次部署流程

### 主机部署流程

1. 安装 MySQL
2. 创建/准备数据库账户
3. 安装软件
4. 打开软件 → 进入“服务器配置”
5. 填写数据库连接信息
6. 填写图片共享目录
7. 点击 **主机初始化**
8. 等待数据库建表、种子数据导入、图片迁移完成
9. 正常登录使用

### 客户端部署流程

1. 安装软件
2. 打开软件 → 进入“服务器配置”
3. 填写主机 IP、数据库信息、共享图片目录
4. 保存并使用
5. 登录使用

## 当前代码已经具备的能力

- 启动时不再自动建表
- 主机可通过“服务器配置”一键初始化数据库
- 支持保存数据库连接配置
- 支持保存图片共享目录
- 支持旧图片路径回退到共享目录
- 支持在主机初始化时迁移历史图片到共享目录

## 后续建议创建的打包文件

建议后面继续补这几个文件：

- `packaging/cadre_archives_client.spec`
- `packaging/cadre_archives.iss`
- `packaging/build_client.ps1`
- `packaging/README.md`

## 需要进一步确认的事项

在真正出安装包前，还需要确认：

- 是否只做 **一个统一安装包**，还是拆成“主机版/客户端版”两个安装包
- 主机端的 MySQL 是要求用户手动安装，还是提供单独部署说明
- 共享图片目录的标准命名是否统一为 `cadre_images`
- 是否需要在安装后自动打开“服务器配置”窗口

## 建议的第一版落地顺序

1. 先确定安装包方案
2. 再补 `PyInstaller spec`
3. 再补 `Inno Setup` 脚本
4. 最后再试打包、修资源路径问题

